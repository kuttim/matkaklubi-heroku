<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
  <%- include ("../partials/header.ejs") %>
</head>

<body>
    <div class="">
      <div class="container-fluid header-section">
        <%- include ("../partials/nav.ejs") %>
  
      </div>
    
    <div class="container text-center">
        <div class="row">
          <div class="col-sm">
            <ul>
                <% treks.forEach(function(trek) { %>
                <li>
                    <a href="<%= trek.url %>" target="_blank">
                        <%= trek.title %>
                    </a>
                    <p>
                        <%= trek.description %>
                    </p>
                    <img src="<%= trek.image %>" alt="<%= trek.title %>" >

                    <p>
                        <%= trek.date %>
                    </p>
                    <p>Asukoht:
                        <%= trek.location %>
                    </p>
                    <p>
                        Algab: <%= trek.startsAt %>
                    </p>
                    <p>
                        Lõppeb: <%= trek.endsAt %>
                    </p>
                </li>
                <% }); %>
        
            </ul>
          </div>
        </div>
      </div>
  </body>
  </html>
  